This is for Windows user:
1/ Run redis-server.exe in REDIS file by clicking on it, keep it opens

This is for MacOS user:
1/ Move to https://redis.io/docs/latest/operate/oss_and_stack/install/install-stack/homebrew/ , follow instructions to download Redis

After running Redis server successfully
2/ Accessing DBMS folder, run 2 terminal windows with command   python hospital.py   and command   python app.py
3/ In app.py terminal window, click on "Running on http://127.0.0.1:5000" to access   
